# Flight-CRUD-website
A Node.js project for managing flight records with MongoDB, enabling CRUD operations like adding, retrieving, updating, and deleting flights. Each flight includes details such as ID, airline, source, destination, fare, and duration. The system is modular, with separate files for database connection, model, controller, and application logic.
